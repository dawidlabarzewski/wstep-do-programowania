#include <stdio.h>

int main() {
    int liczba = 10;

    int dwukrotnosc = liczba * 2;

    int kwadrat = liczba * liczba;

    printf("liczba: %d\n", liczba);
    printf("dwukrotnosc: %d\n", dwukrotnosc);
    printf("liczba do kwadratu: %d\n", kwadrat);

    return 0;
}